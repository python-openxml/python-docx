language: python
python:
  - "3.6"
  - "3.5"
  - "2.7"
  - "2.6"
# command to install dependencies, e.g. pip install -r requirements.txt --use-mirrors
install: pip install -r requirements.txt
# command to run tests, e.g. python setup.py test
script:  py.test

notifications:
  email:
    recipients:
      - ondrej.zoricak.2@student.tuke.sk
    on_success: always
    on_failure: always